## versions

```
transformers==4.57.3
```

## Lambda Instance Setup Instructions

1. Create instance with version: `Lambda Stack 22.04`

2. Run command: `git clone https://github.com/aklein4/ml-skeleton.git`

3. Run command: `cd ~/ml-skeleton && . setup_vm.sh <WANDB_TOKEN>`
